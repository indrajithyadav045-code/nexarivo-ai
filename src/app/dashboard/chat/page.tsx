import { models, agents } from '@/lib/config';
export default function ChatPage(){return <section className="card"><h1>Chat</h1><div className="grid-auto"><select>{models.map(m=><option key={m.id}>{m.name}</option>)}</select><select>{agents.map(a=><option key={a}>{a}</option>)}</select></div><div style={{display:'grid',gap:14,margin:'20px 0'}}><div className="chat-bubble">Upload documents, ask questions, request code, or run research.</div><div className="chat-bubble">Streaming API route supports model switching, markdown, code, retry, and regeneration client controls.</div></div><textarea rows={5} placeholder="Message NEXARIVO..."/><button className="btn btn-primary" style={{marginTop:12}}>Send</button></section>}
